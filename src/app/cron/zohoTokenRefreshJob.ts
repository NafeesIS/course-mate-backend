// import { refreshAccessToken } from '../modules/zoho/zoho.service';

// const startTokenRefreshJob = async () => {
//   // Immediately refresh the token when the service starts
//   await refreshAccessToken();

//   // Refresh the token every 59 minutes (3540 seconds)
//   // setInterval(refreshAccessToken, 3540 * 1000);
//   setInterval(refreshAccessToken, 5 * 1000);
// };

// // Start the job when the application starts
// startTokenRefreshJob();
